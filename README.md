The python script assumes you have the emails in a file called sbu.csv. It will output the emails in a file called "selected_emails.csv". The JS file assumes your email file is called "selected_emails.csv". 

Run the python file BEFORE running the js file. 

Run "sudo chmod a+x install.sh" and then "sudo ./install.sh" to install npm dependencies 
